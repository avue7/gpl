string strings[3];

on akey
{
    strings[0] = "zero";
    strings[1] = "one";
}

on skey
{
    print(strings[1]);
    //print("strings[0] = " + strings[0]);
    //print("strings[1] = " + strings[1]);
}
